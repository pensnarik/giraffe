version: '2'

networks:
  giraffe:

services:
  database:
    image: giraffe
    environment:
      PG_LOG_MIN_MESSAGES: 'info'
      PG_LOG_MIN_DURATION_STATEMENT: '0'
    cpuset: 0,1
    mem_limit: 512M
    networks:
      - giraffe
    ports:
      - '20000:5432'
